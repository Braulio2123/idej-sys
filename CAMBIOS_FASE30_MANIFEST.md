# Manifest de cambios · Fase 30

Este ZIP contiene únicamente archivos nuevos o modificados. En el ZIP completo corregido ya no existe `database/factories/UserFactory.php`; debe eliminarse del proyecto porque referencia el modelo estándar `User` y el sistema interno usa `App\Models\Usuario`.

No se modificó Portal Alumno.

No se agregaron migraciones nuevas.
